# Working progers

# Types of variables

<table>
    <tbody>
        <tr>
            <td>
            <strong>Tag:</strong>
            </td>
            <td>
                <img alt="variables" src="https://img.shields.io/badge/-variables-green">
                <img alt="string-lib" src="https://img.shields.io/badge/-string-green">
            </td>
        </tr>
    </tbody>
</table>

Select the languages: [English](#english) | [Italiano](#italiano)

# English

🛠️ working progress 🛠️

# Italiano

## **Introduzione e spiegazione:**

In questo programma ho elencato la maggior parte delle tipologie di variabili con degli esempi, avevo già introdoto il funzionamento delle variabili nel *file* [`Hello_world.cpp`](https://github.com/Thealexio-exe/learn-cpp-by-projects/blob/main/1-Easy/1-Hello-world/Hello_world.cpp) , ci sono alcuni tipo di variabili che ho voluto omettere per esempio `long` o `signed/unsigned`, che sono concetti complessi da imparare e utilizzata in ambiti più specifici.

## **Soluzioni:**

<details>
<summary>Clicca per vedere le soluzioni</summary>

Il primo errore in questo programma si trova nelle variabili di tipo *decimale*:

```cpp
    double var_double = 2,71828; --> double var_double = 2.71828;
```

come puoi notare ho già coretto l'esercizio, l'errore e in se è semplice, non usare la `,` ma il `.` per i numeri decimali <br>
Il secondo invece si trova nelle variabili di tipo *bool*:

```cpp
    bool var_bool = 1; --> bool var_bool = false;
```

l'errore è semplice, le variabili di tipo *bool* accettano solo due valori `true` e `false`

🛠️ working progress 🛠️

</details>